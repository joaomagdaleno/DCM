name: dqcm
description: A Dart Quality Control Metrics tool.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.0

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  analyzer: ^9.0.0
  args: ^2.7.0
  yaml: ^3.1.3
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
